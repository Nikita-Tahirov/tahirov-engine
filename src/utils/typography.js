/**
 * Типограф: устраняет висячие предлоги.
 * Заменяет пробел после коротких слов (1-2 буквы) на неразрывный пробел.
 */
export function typo(text) {
    if (!text) return '';
    return text.replace(/(\s|^)([а-яА-ЯёЁa-zA-Z]{1,2})\s+/g, '$1$2&nbsp;');
}